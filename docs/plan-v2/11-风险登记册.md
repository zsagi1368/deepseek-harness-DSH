# 11 · 风险登记册

> ORCH 每次 STATE.md 更新时复核一次；触发条件命中即执行对应预案。概率/影响：高/中/低。

| ID | 风险 | 概率 | 影响 | 触发信号 | 缓解（事前） | 应急（事后） |
|---|---|---|---|---|---|---|
| R-1 | 官方在执行期再发版（0.1.1-rc.3+），改动砸中 credentials/llm 热区 | 中 | 高 | `git fetch upstream` 出现新 tag / release 提交 | 05 §8：不中断当前 Phase，同步窗口统一处理 | 提前做 03 §4 同步；强冲突时 ORCH 决策是否冻结基线重走 T1.3 冲突裁决（已有 adjudication.md，成本可控） |
| R-2 | T1.3 三方合并冲突超出裁决表（尤其 credentials 官方重做） | 中 | 高 | `git apply -3` 冲突且裁决规则不适用 | 冲突优先级规则（03 §2.2）+ ARCH 前置研究官方新实现 | 停线会诊；该文件转 redo/deferred，不硬并；必要时该修复降级 backlog |
| R-3 | 治理包在官方 per-file 100% 覆盖门下补测工作量超预期 | 中 | 中 | T2.2 覆盖报告 <100% 项多 | 任务书已允许带因 ignore；范围收敛到核心路径 | 与 REV 协商按"核心路径 100%、其余显式 ignore+backlog"放行，债务登记 |
| R-4 | CordisAdapter 阶段 1 发现 4 个 P0 之一无法以 adapter 缓解 | 中 | 高 | T2.3 集成测试构造不出合法包装 | 阶段 1 边界已在任务书限定为"可加载官方示例插件" | 缩小阶段 1 承诺（如实记录到 plugin-compat.md），把不可缓解项升级为方案修订议题（ORCH+ARCH），不阻塞其余 Phase |
| R-5 | Windows 环境跑不全官方门禁（Linux-only 工具链：landlock、wine 门） | 高 | 中 | T1.4/T3 门禁出现环境性失败 | 任务书允许"官方 CI 对照的证据式豁免"；官方有 Windows 门子集 | 豁免清单制；关键门（build/test/typecheck）绝不通融，必要时用 GitHub Actions 自身作为门禁执行环境 |
| R-6 | 凭据/敏感信息再次入库 | 低 | 高 | lefthook/SEC 抽查发现 | 10 §4 红线 + lefthook；交接记录模板无凭据字段 | 立即叫停推送（SEC 权限）；轮换凭据；评估是否需历史清除 |
| R-7 | 移植遗漏（add/mod 清单不全导致功能丢失） | 低 | 高 | 事后发现旧线某能力缺失 | T1.2 "宁多勿漏"+ 三清单可复核；`diff-with-official` 基线核对 | 从 archive/our-base-v1 找回（一切旧物都在归档线）；补 T2.x 增量任务 |
| R-8 | 范围蔓延（memory 包、三核心插件、阶段 2–4 被顺手做） | 中 | 中 | 任务交接记录出现范围外改动 | 04 §5 backlog 制；REV 检查单"无搭车改动" | 拆出独立分支回退搭车部分，登记 backlog |
| R-9 | 多代理并行改同一包产生覆盖/冲突 | 中 | 中 | STATE.md 冲突区占用违规 | 包级互斥（05 §1.3）+ 功能分支隔离 | ORCH 仲裁按时间序保留后提交者 rebase；强化占用登记 |
| R-10 | 用户决策点（PAT 轮换、默认分支、force push、发布推送）等待阻塞 | 中 | 低 | T0.1/T1.5/T5.3 挂起超过一个工作日 | 决策点已前置列明（05 §3）；均有不依赖用户的默认路径（方案 B） | 走默认路径先行，决策点留待用户事后确认（force push 除外——绝不通融） |
| R-11 | 旧线有价值信息在归档中失焦（如三核心插件源码、23 bug 修复证据） | 中 | 低 | T4.2 映射表发现大量"查无此物" | 映射表本身就是排查动作；旧 tag/分支永不删除 | 按映射表缺口逐项从 `0.1.0-rc.8-DSH20260820a`/archive 线考古补录 |
| R-12 | 官方 CI 在 fork 仓库缺 secrets/runner（e2b、发布链）长期红 | 高 | 低 | Actions 里环境性失败固化 | T3.1 豁免清单 | 豁免清单常态化并写入 docs/dsh/FAQ，避免每次解释 |
| R-13 | our/v2 与 archive 线并行期间误操作旧线 | 低 | 中 | archive 上出现新提交 | 归档即推送快照；our/base 保留只读 | 从远端快照恢复；STATE.md 记录事故与补丁 |

## 历史教训 → 规则映射（为什么有这些红线）

| 旧事故（01 号报告） | 对应规则 |
|---|---|
| 快照式孤儿分支 → 无法同步官方 | 03 §2 一次性重建 + §4 常规 SOP |
| 死代码交付（无 package.json） | 02 D3、T2.1 |
| 空提交冒充修复、报告互相矛盾 | 05 §7 证据制交接、10 §3 双人制 |
| README 重写破坏 i18n 契约 | 02 D8、T4.3 |
| PAT 明文两处 | 10 §4.1、T0.1/T0.2 |
| 构建产物 11,200 文件入库 | 03 §7、T1.1 |
| tag 滞后、安装指引失真 | 03 §5、T5.2 |
